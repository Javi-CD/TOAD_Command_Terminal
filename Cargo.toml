[package]
name = "terminal_simulada"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = { version = "0.25.0", features = ["persistence"] }
egui = "0.25"
winapi = { version = "0.3.9", features = ["winuser", "windef"] }